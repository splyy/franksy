{% extends 'base.html.twig' %}

{% block title %}Hello {{ name }}!{% endblock %}

{% block body %}
<div class="min-h-screen gradient-bg flex items-center justify-center p-4 md:p-8 relative overflow-hidden">
    {# Animated background orbs #}
    <div class="absolute top-20 left-20 w-96 h-96 bg-purple-400 rounded-full mix-blend-multiply filter blur-3xl opacity-60 animate-float"></div>
    <div class="absolute bottom-20 right-20 w-96 h-96 bg-pink-400 rounded-full mix-blend-multiply filter blur-3xl opacity-60 animate-float" style="animation-delay: 3s;"></div>

    <div class="max-w-3xl w-full relative z-10">
        {# Main Card with Glassmorphism #}
        <div class="card-glass">
            {# Animated Wave Emoji #}
            <div class="text-center mb-8">
                <div class="inline-block text-9xl animate-float mb-4">
                    👋
                </div>
            </div>

            {# Gradient Title with Pulse Glow #}
            <h1 class="text-6xl md:text-7xl font-black text-center mb-8 animate-pulse-glow">
                <span class="gradient-text drop-shadow-lg">
                    Hello, {{ name }}!
                </span>
            </h1>

            {# Info Section #}
            <div class="card bg-white/80 mb-8">
                <p class="text-lg md:text-xl text-center text-gray-700 leading-relaxed mb-6">
                    Cette page est servie par <strong class="gradient-text">FrankenPHP</strong> en mode worker<br class="hidden md:block">
                    avec <strong class="gradient-text">Symfony 8.0</strong> et <strong class="gradient-text">PHP 8.5</strong>
                </p>

                {# Tech Badges Grid #}
                <div class="grid grid-cols-1 md:grid-cols-3 gap-3">
                    <div class="bg-gradient-to-br from-purple-500 to-indigo-600 rounded-xl p-4 text-center transform hover:scale-105 transition-all duration-300 shadow-lg">
                        <div class="text-3xl mb-2">⚡</div>
                        <div class="text-white font-bold text-sm">Worker Mode</div>
                        <div class="text-white/80 text-xs mt-1">Ultra rapide</div>
                    </div>

                    <div class="bg-gradient-to-br from-pink-500 to-rose-600 rounded-xl p-4 text-center transform hover:scale-105 transition-all duration-300 shadow-lg">
                        <div class="text-3xl mb-2">🐘</div>
                        <div class="text-white font-bold text-sm">PHP 8.5</div>
                        <div class="text-white/80 text-xs mt-1">Dernière version</div>
                    </div>

                    <div class="bg-gradient-to-br from-indigo-500 to-purple-600 rounded-xl p-4 text-center transform hover:scale-105 transition-all duration-300 shadow-lg">
                        <div class="text-3xl mb-2">🚀</div>
                        <div class="text-white font-bold text-sm">Symfony 8.0</div>
                        <div class="text-white/80 text-xs mt-1">Framework moderne</div>
                    </div>
                </div>
            </div>

            {# Performance Stats #}
            <div class="grid grid-cols-3 gap-4 mb-8">
                <div class="text-center p-4 bg-white/50 backdrop-blur-sm rounded-xl border border-white/30">
                    <div class="text-3xl font-black gradient-text">99%</div>
                    <div class="text-xs text-gray-700 font-semibold mt-1">Performance</div>
                </div>
                <div class="text-center p-4 bg-white/50 backdrop-blur-sm rounded-xl border border-white/30">
                    <div class="text-3xl font-black gradient-text">&lt;1s</div>
                    <div class="text-xs text-gray-700 font-semibold mt-1">Temps réponse</div>
                </div>
                <div class="text-center p-4 bg-white/50 backdrop-blur-sm rounded-xl border border-white/30">
                    <div class="text-3xl font-black gradient-text">100%</div>
                    <div class="text-xs text-gray-700 font-semibold mt-1">Uptime</div>
                </div>
            </div>

            {# Back Button #}
            <div class="text-center">
                <a href="/" class="btn-secondary inline-flex items-center gap-2 group">
                    <span class="group-hover:-translate-x-1 transition-transform duration-200">←</span>
                    <span>Retour à l'accueil</span>
                </a>
            </div>
        </div>

        {# Floating Action Cards #}
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mt-6">
            <a href="/" class="card-glass text-center p-4 hover:scale-110 transition-all duration-300 group">
                <div class="text-3xl mb-2 group-hover:animate-bounce">🏠</div>
                <div class="text-white text-sm font-semibold">Home</div>
            </a>
            <a href="/hello/Symfony" class="card-glass text-center p-4 hover:scale-110 transition-all duration-300 group">
                <div class="text-3xl mb-2 group-hover:animate-bounce">🎯</div>
                <div class="text-white text-sm font-semibold">Demo</div>
            </a>
            <a href="#" class="card-glass text-center p-4 hover:scale-110 transition-all duration-300 group">
                <div class="text-3xl mb-2 group-hover:animate-bounce">📖</div>
                <div class="text-white text-sm font-semibold">Docs</div>
            </a>
            <a href="#" class="card-glass text-center p-4 hover:scale-110 transition-all duration-300 group">
                <div class="text-3xl mb-2 group-hover:animate-bounce">⚙️</div>
                <div class="text-white text-sm font-semibold">Config</div>
            </a>
        </div>
    </div>
</div>
{% endblock %}
